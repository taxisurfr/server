export const newLogin = (username) => {
    return {
        type: 'NEW_LOGIN',
        username
    }
}

export const newFilms = (films) => {
    return {
        type: 'NEW_LOGIN',
        films
    }
}
